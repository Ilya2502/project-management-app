export type ColumnsInBoardResponseType = {
    _id: string,
    title: string,
    order: number,
    boardId: string,
}

export type NewColumnType = {
    title: string,
    order: number,
}

export type ColumnResponseType = {
    _id: string,
    title: string,
    order: number,
    boardId: string,
};
